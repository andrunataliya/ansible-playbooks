---
- name: Loop
  hosts: 54.70.144.41
  become: true
  become_method: sudo
  connection: ssh
  ignore_errors: false
  tasks:
    - name: Loop
      package: 
        name: "{{item}}"
        state: present
      with_items:
        - epel-release
        - php
        - wget
        - unzip
        - mariadb-server
        - mysql


     - name: Command
       command: "{{item}}"